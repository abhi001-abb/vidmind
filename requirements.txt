# ── Web framework ──────────────────────────────────────────────────────────
Flask>=3.0,<4.0
Flask-Limiter>=3.5,<4.0          # per-IP rate limiting on /process (falls
                                  # back to a built-in limiter in app.py if
                                  # this package isn't installed, so it's
                                  # not a hard requirement to run the app)

# ── Config ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0,<2.0

# ── Video / audio ──────────────────────────────────────────────────────────
moviepy>=1.0.3,<2.0               # pinned <2.0: moviepy 2.x renamed the
                                   # `moviepy.editor` import used in
                                   # video_to_audio.py — upgrading needs a
                                   # code change, not just a version bump.
imageio-ffmpeg>=0.6.0

# ── Transcription ──────────────────────────────────────────────────────────
openai-whisper>=20231117
torch>=2.0                        # whisper's actual runtime dependency.
                                   # CPU-only installs can instead use the
                                   # index at https://download.pytorch.org/whl/cpu
                                   # to avoid pulling a multi-GB CUDA build.

# ── Summarization / quiz (Gemini) ───────────────────────────────────────────
google-generativeai>=0.8.0,<1.0

# ── YouTube download ─────────────────────────────────────────────────────────
yt-dlp>=2024.1.1                  # keep this updated regularly — YouTube's
                                   # bot-detection changes frequently and
                                   # older yt-dlp builds break first.
gunicorn>=23.0.0